"use client";

interface ClaimTokensModalProps {
  isOpen: boolean;
  onClose: () => void;
  listingId: string;
  tokenAmount: string;
  vehicleName: string;
}

export const ClaimTokensModal = (_props: ClaimTokensModalProps) => {
  return null;
};
