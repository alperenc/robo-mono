"use client";

interface ClaimRefundModalProps {
  isOpen: boolean;
  onClose: () => void;
  listingId: string;
  refundAmount: string;
  vehicleName: string;
}

export const ClaimRefundModal = (_props: ClaimRefundModalProps) => {
  return null;
};
