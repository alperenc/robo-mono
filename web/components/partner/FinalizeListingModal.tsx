"use client";

interface FinalizeListingModalProps {
  isOpen: boolean;
  onClose: () => void;
  onSuccess?: () => void;
  listingId: string;
  tokenAmount: string;
}

export const FinalizeListingModal = ({}: FinalizeListingModalProps) => {
  return null;
};
